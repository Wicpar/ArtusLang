package artus.lang

import artus.lang.Spaceable
import artus.lang.Name

interface inNamespace(path: Array<String>);

token namespace: interlace(allspace), inNamespace = "namespace" name "{" inNamespace(path + [name[0]])* "}";