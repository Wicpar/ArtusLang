###
    var import = function(path, type, charset) {
        log:println(folder);
        if (charset != null) {
            lang:import(folder + "/" + path, type ?: context.type, charset);
        } else {
            lang:import(folder + "/" + path, type ?: context.type);
        }
    };
    this:set("import", import);
###